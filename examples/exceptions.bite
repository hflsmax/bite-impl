eff Exc : ∀[].∀{}.() -> int_[];;

let run =
    fun g [] {} (n : int) : int_[] is
        handle lexc : Exc = fn [] {} () : int_[] is 0 end in
            if n = 0 then
                raise lexc ()
            else
                g (n-1)
        end
    end
in
    Print (run 10)
end
;;