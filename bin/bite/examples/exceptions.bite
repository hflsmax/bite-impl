eff Exc : ∀[].∀{}.() -> int_[];;

let run =
    fun g [] {} (n : int) : int_[] is
        handle lexc : Exc = fun exch [] {} () : int_[] is 0 end in
            if n = 0 then
                raise lexc ()
            else
                g (n-1)
        end
    end
in
    run 10
end
;;